Available images for OpenSTLinux layers are:

  - The system based on OpenSTLinux design supports YA157C series products

      100ask-image-weston       -   OpenSTLinux weston image with basic Wayland support (if enable in distro)
     


  - The system based on QT framework 

      100ask-image-qt	      -   MYiR HMI demo of image based on QT framework   (require 'openstlinux-eglfs' distro)


  - Other OpenSTLinux images:

      100ask-image-core         -   OpenSTLinux core image
      		and more images are available on meta-100ask-st/recipes-100ask/images.
